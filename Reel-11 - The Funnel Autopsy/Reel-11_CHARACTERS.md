# Wajdan — Reel #11 "The Funnel Autopsy" — CHARACTERS

Diagnostic reel (three-leaks structure — mostly diagram scenes with two human/mascot beats). Reused
Wajdan cast — **no new sheets**. Consistency via attaching sheets as image references (`imagen-nano-banana-2`).

## Reused (from Reel-08 / Reel-10)
| Character | File | Look (locked) | Used in |
|-----------|------|---------------|---------|
| **The Owner ("You")** | `Characters/Owner.png` | messy black hair, stubble, tired eyes, heather-grey tee, charcoal jeans | SC-01 (bleeding funnel at his desk), SC-06 (the bottleneck) |
| **SYS** (the system robot) | `Characters/SYS-robot.png` | rounded cream body, gear on chest, antenna with orange light, claw hands | SC-02 (the autopsy / diagnostician), SC-11 (seals the 3 leaks), SC-13 (nothing waits on a human), SC-15 (AUDIT CTA) |

## Not used
- Michael, Team — this is the self-diagnosis "autopsy", not the You-vs-Michael story.

## Brand thread (Wajdan)
- **Orange `#E07B00`** = the system / good / working accent (glows, the machine, automatic badges, SYS antenna, sealed leaks, booked slots).
- **Grey** = the leak / dead / grind accent (slow-reply clock, dropped leads, the cracks, "goes quiet").
- Keep humans naturally coloured; the orange lives in the SYSTEM props + glows + captions.
- Handle `@wajdan.co` bottom-center; WAJDAN wordmark logo top-left (editor).

## Prop kit (keep consistent — see Reel-08 / Reel-10 for the full kit)
- **Lead** = envelope with a tiny person (hot = orange glow + ✓, dead = grey + ✗).
- **The leaking funnel** = a funnel with three visible cracks, grey lead-envelopes dripping out — the
  hero prop of this reel. Each crack = one leak.
- **Slow-reply clock** = a grey ticking clock beside a silent chat bubble.
- **Bottleneck** = a pipe pinched narrow where a single "you" figure is stuck as the plug.
- **CRM safety-net** = orange net catching envelope-leads (the fix).
- **Auto badge** = a small round orange badge with a circular-arrows / lightning "AUTO" mark.
- **Calendar** = clean orange "BOOKED" tags; booking link = one clear orange button.

## Reference-passing rule
- OWNER / SYS scenes → `imagen-nano-banana-2` + that sheet (single character, a normal scene, NOT an
  expression sheet, no mood/pose labels).
- OBJECT / diagram scenes → `recraft-v4-1`, no reference.

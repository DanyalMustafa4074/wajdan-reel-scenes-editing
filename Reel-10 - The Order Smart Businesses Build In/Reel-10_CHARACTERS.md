# Wajdan — Reel #10 "The Order Smart Businesses Build In" — CHARACTERS

Framework/system reel (mostly diagram scenes). Reused Wajdan cast — **no new sheets**. Consistency via
attaching sheets as image references (`imagen-nano-banana-2`).

## Reused (from Reel-08)
| Character | File | Look (locked) | Used in |
|-----------|------|---------------|---------|
| **The Owner ("You")** | `Characters/Owner.png` | messy black hair, stubble, tired eyes, heather-grey tee, charcoal jeans | SC-02 (bad calendar), SC-14 (funding a leak) |
| **SYS** (the system robot) | `Characters/SYS-robot.png` | rounded cream body, gear on chest, antenna with orange light, claw hands | SC-05 (blueprint), SC-09 (CRM/automation), SC-15 (GUIDE) |

## Not used
- Michael, Team — this is the "system order" explainer, not the You-vs-Michael story.

## Brand thread (Wajdan)
- **Orange `#E07B00`** = the system / good / working accent (glows, the machine, booked slots, SYS antenna).
- **Grey** = the leak / dead / grind accent (no-shows, dripping $, the broken half).
- Keep humans naturally coloured; the orange lives in the SYSTEM props + glows + captions.
- Handle `@wajdan.co` bottom-center; WAJDAN wordmark logo top-left (editor).

## Prop kit (keep consistent — see Reel-08 for the full kit)
Lead = envelope w/ tiny person (hot = orange glow+✓, dead = grey+✗); ADS box (∞ swirl + $);
CRM = safety-net catching envelopes; Calendar = green "BOOKED" tags; leaking funnel = cracks dripping
$; the System machine = ADS→CRM→follow-up→CALENDAR boxes joined by arrows, glowing orange.

## Reference-passing rule
- OWNER / SYS scenes → `imagen-nano-banana-2` + that sheet (single character, NOT an expression sheet).
- OBJECT/diagram scenes → `recraft-v4-1`, no reference.
